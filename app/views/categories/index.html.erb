<h1>Categories</h1>

<%#<p>
  <%= link_to "New Category", new_category_path, class:"btn btn-primary" %>
<%#</p> %>

<thead class="table table-striped">
  <tr>
    <th><%= link_to "Home", root_path, class:"btn btn-secondary" %></th>
    <th><%= link_to "New Category", new_category_path, class:"btn btn-primary" %></th>
  </tr>
</thead>
<br><br>
<table class="table table-striped table-hover table-bordered">
  <thead>
    <tr>
      <th>Name</th>
      <th>Actions</th>
    </tr>
  </thead>

  <tbody>
    <% @categories.each do |category| %>
      <tr>
        <td><%= category.name %></td>
        <td>
          <%= link_to 'Show', category, class:"btn btn-info" %>
          <%= link_to 'Edit', edit_category_path(category), class:"btn btn-warning" %>  
          <%= link_to 'Destroy', category, method: :delete, data: { confirm: 'Are you sure?' }, class:"btn btn-danger" %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
  <%= link_to "Home", root_path, class:"btn btn-light" %>

<br>
